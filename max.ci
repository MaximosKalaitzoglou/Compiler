program MAX

declare a,b,c,d,e;
function max(in x, in y)
{

    if (not[x>y])
    {
    return(x);

    };

    else
    {
    return(y);
    };
}

{
    a := 4;
    b := 3;
    input(c);
    input(d);
    e := max(in max(in max(in a, in b),in d),in c);
    print(e);
}.